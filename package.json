{
  "name": "diffnoun",
  "version": "0.1.0",
  "description": "Name added, removed, and changed files from a unified diff.",
  "bin": {
    "diffnoun": "./src/cli.js"
  },
  "main": "./src/index.js",
  "scripts": {
    "test": "node --test"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
